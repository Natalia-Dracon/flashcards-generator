# 🧠 Интерактивные Flashcards с ИИ

Этот проект представляет собой простое веб-приложение для генерации учебных карточек (flashcards) по заданной теме, используя модель GPT-3.5-turbo от OpenAI.

## ⚠️ ВАЖНО: Настройка API Ключа

Для работы приложения требуется **действующий ключ OpenAI API**. **КАТЕГОРИЧЕСКИ НЕ РЕКОМЕНДУЕТСЯ** хранить этот ключ в файле `script.js` при размещении проекта на GitHub.

### 🔒 Рекомендуемый способ (для развертывания)

Используйте **Прокси-сервер** или **Бессерверную функцию (например, Netlify Functions, Vercel Edge Functions)**, чтобы скрыть ваш API-ключ.

В этом случае вам нужно:
1.  Создать бэкенд (Node.js/Python и т.д.), который принимает POST-запрос с `prompt`.
2.  На бэкенде использовать ваш `OPENAI_API_KEY` для вызова `https://api.openai.com/v1/chat/completions`.
3.  Изменить константу `API_ENDPOINT` в `script.js` на URL вашего прокси-сервера.

### 🛠️ Локальный способ (только для тестирования)

Если вы запускаете проект **ТОЛЬКО ЛОКАЛЬНО** и понимаете риски, вы можете временно добавить ключ в `script.js`:

1.  Получите ваш ключ с сайта OpenAI.
2.  **Замените** весь блок кода запроса в `script.js` на оригинальную реализацию, но с ключом в переменной `OPENAI_API_KEY`.